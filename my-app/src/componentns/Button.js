const Button = ({title}) =>{
    //const {title} = props
    return(<button>{title}</button>)

}

export default Button

// const Button = ({children}) =>{
//     return <button>{children}</button>
// } //darosi wrapam arba paprastam tekstam (app.js)